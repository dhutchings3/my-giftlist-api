CREATE TABLE IF NOT EXISTS giftlist_lists (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  listcode TEXT NOT NULL UNIQUE,
  listname TEXT,
  username TEXT REFERENCES giftlist_users(id) NOT NULL,
  items TEXT NOT NULL
);
