CREATE TABLE IF NOT EXISTS giftlist_list (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id INTEGER REFERENCES giftlist_users(id) ON DELETE CASCADE NOT NULL,
  item_id INTEGER REFERENCES giftlist_items(id) ON DELETE CASCADE NOT NULL
)